name: Build Test
description: 'Build Test'

inputs:
  json-payload:
    description: 'Test'
    required: false
    default: ''

runs:
  using: 'composite'
  steps:
    - name: Check out the repository
      uses: actions/checkout@v4
    - name: Build Test
      shell: bash
      run: |
        echo "Test : ${{ toJson(context) }}"