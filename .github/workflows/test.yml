name: Test package again

on: [workflow_dispatch]

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Unity empty project
        uses: actions/checkout@v3
      - uses: homapf/github-actions-tests/.github/actions/add-registry.yml@main
        with:
          registry-url: "some-url"
          registry-name: "some-name"
          registry-scope: "some-scope"
