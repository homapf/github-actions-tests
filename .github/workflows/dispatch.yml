name: Run Unity

on:
  workflow_dispatch:
  
jobs:
  run-unity:
    runs-on: ubuntu-latest

    name: Run Unity
    steps:
      - name: Test
        env:
          TEST: "Hello World"
        uses: ./